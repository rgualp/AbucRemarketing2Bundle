<table class="table table-striped table-hover">
    <thead>
    <tr>
        <th>Fecha del pago</th>
        <th>Tipo de Cancelación</th>
        <th>Nombre y Correo del cliente</th>
        <th>Monto del Pagar</th>
        <th>Estado del pago</th>
        <th>Usuario</th>
        <th>Fecha</th>

    </tr>
    </thead>
    <tbody>
    {% for pay in pays %}
        <td>{{ pay.getPaymentDate | date("d/m/Y") }}</td>
        <td>{{ pay.getCancelId.getType.getCancelName }}</td>.
        <td><b>Nombre Cliente (Turista) :</b>{{ pay.getUserTourist.getUserTouristUser.getUserCompleteName }} - <b>Correo:</b>{{ pay.getUserTourist.getUserTouristUser.getUserEmail }}</td>
        <td>{{ pay.getPayAmount }} {{ pay.getCancelId.getBooking.getBookingCurrency.getCurrSymbol }} </td>
        <td>{{ pay.getType.translations[0].nomLangDescription }}</td>
        <td>{{ pay.getUser.getUserCompleteName }}</td>
        <td>{{pay.registerDate | date ("d/m/Y")}}</td>
    {% endfor %}
    </tbody>
</table>

