{% extends 'mycpBundle:layout:lodging.html.twig' %}
{% block title %}Administración{% endblock %}
{% block top_body %}
<ul class="breadcrumb">
    <li><a href="{{path('mycp_lodging_front')}}">Administración</a> <span class="divider">/</span></li>
    <li class="active">Reservas</li>
</ul>
{% endblock %}
{% block helpContent %}
<p>Esta sección muestra el listado de todas las reservaciones que ha recibido su propiedad a través de MyCasaParticular.</p>
<p>Puede realizar búsquedas en el listado utilizando el formulario que aparece en el recuadro gris justo encima del listado.</p>
<p>Cada reservación posee un botón "Detalles" a la derecha. Si acciona sobre este, se visualizará una página que contiene la información de la reservación seleccionada.</p>
{% endblock %}
{% block content %}
<h2>Reservas</h2>
<hr/>
<ul class="nav nav-tabs" id="myTab">
    <li class="active"><a data-toggle="tab" href="#reservas">Reservas</a></li>
    <li class=""><a href="{{path("mycp_list_readonly_reservations_user")}}">Clientes</a></li>
</ul>
    <div class="tab-content" id="myTabContent">
        <div id="reservas" class="tab-pane fade active in">

            {% for message in app.session.flashbag.get('message_error_local') %}
            <div class="alert alert-error">{{ message }}</div>
            {% endfor %}
            <form onkeypress="javascript:submit_filters(event);" id="filter" method="post" action="{{path('mycp_list_readonly_reservations')}}">
                <div class="well well-small">
                    <div class="row-fluid">
                        <div class="span6"><label>Número de reserva:</label>
                            <input id="filter_offer_number" name="filter_offer_number" type="text" class="input-block-level" value="{{filter_offer_number}}"/>
                        </div>
                            <div class="span6"><label>Estado reserva:</label>
                            <select id="filter_status" name="filter_status" class="input-block-level">
                                <option></option>
                            {{ render(controller('mycpBundle:BackendReservation:get_general_reservation_status', {'post': {'selected':filter_status}})) }}
                            </select>
                        </div>



                    </div>
                   <div class="row-fluid">
                        <div class="span3"><label>Fecha reserva:</label>
                            <input id="filter_date_reserve" name="filter_date_reserve" type="text" class="datepicker input-block-level" value="{{filter_date_reserve}}"/>
                        </div>
                       <div class="span3"><label>Fecha de llegada:</label>
                            <input id="filter_date_from" name="filter_date_from" type="text" class="datepicker input-block-level" value="{{filter_date_from}}"/>
                        </div>
                        <div class="span3"><label>Fecha de salida:</label>
                            <input id="filter_date_to" name="filter_date_to" type="text" class="datepicker input-block-level" value="{{filter_date_to}}"/>
                        </div>
                        <div class="span3">
                            <label>&nbsp;</label>
                            <div class="row-fluid">
                                <div class="span6">
                                    <a rel="tooltip" title="Filtrar listado" href="javascript:action_submit();" class="tooltip_msg btn btn-block btn-success"><i class="icon-filter icon-white"></i></a>
                                </div>
                                <div class="span6">
                                    <a rel="tooltip" title="Resetear filtrado " href="{{ path('mycp_list_readonly_reservations') }}" class="tooltip_msg btn btn-block btn-danger"><i class="icon-remove icon-white"></i></a>
                                </div>
                            </div>
                        </div>
                   </div>
                </div>

            {% if reservations %}
            <div class="alert">
                <div class="row-fluid">
                    <div class="pull-left">
                        Mostrando del {{items_per_page * current_page - items_per_page}} al {% if items_per_page * current_page >  total_items%}{{total_items }}{% else %}{{ items_per_page * current_page }}{% endif %} de {{total_items}} registros
                    </div>
                    <div class="pull-right">
                        Mostrar:
                        {% if filter_date_reserve ==''%}{% set filter_date_reserve='null'%}{%endif%}
                        {% if filter_offer_number ==''%}{% set filter_offer_number='null'%}{%endif%}
                        {% if filter_reference ==''%}{% set filter_reference='null'%}{%endif%}
                        {% if filter_date_from ==''%}{% set filter_date_from='null'%}{%endif%}
                        {% if filter_date_to ==''%}{% set filter_date_to='null'%}{%endif%}
                        {% if filter_date_reserve_twig ==''%}{% set filter_date_reserve_twig='null'%}{%endif%}
                        {% if filter_date_from_twig ==''%}{% set filter_date_from_twig='null'%}{%endif%}
                        {% if filter_date_to_twig ==''%}{% set filter_date_to_twig='null'%}{%endif%}
                        {% if filter_status ==''%}{% set filter_status='null'%}{%endif%}
                        {% if sort_by=='' %}
                           {% set sort_by=0 %}
                        {% endif %}
                        <a class="{% if items_per_page == 100 %}selected{% endif %}" href="{{ path('mycp_list_readonly_reservations',{'items_per_page':100, 'filter_booking_number':'null','filter_date_reserve':filter_date_reserve_twig,'filter_offer_number':filter_offer_number,'filter_reference':filter_reference,'filter_date_from':filter_date_from_twig,'filter_date_to':filter_date_to_twig,'filter_status':filter_status,'sort_by':sort_by})}}">100</a>
                        <a class="{% if items_per_page == 300 %}selected{% endif %}" href="{{ path('mycp_list_readonly_reservations',{'items_per_page':300, 'filter_booking_number':'null','filter_date_reserve':filter_date_reserve_twig,'filter_offer_number':filter_offer_number,'filter_reference':filter_reference,'filter_date_from':filter_date_from_twig,'filter_date_to':filter_date_to_twig,'filter_status':filter_status,'sort_by':sort_by})}}">300</a>
                        <a class="{% if items_per_page == 500 %}selected{% endif %}" href="{{ path('mycp_list_readonly_reservations',{'items_per_page':500, 'filter_booking_number':'null','filter_date_reserve':filter_date_reserve_twig,'filter_offer_number':filter_offer_number,'filter_reference':filter_reference,'filter_date_from':filter_date_from_twig,'filter_date_to':filter_date_to_twig,'filter_status':filter_status,'sort_by':sort_by})}}">500</a>
                        <input class="hidden" name="items_per_page" value="100">
                    </div>
                </div>
            </div>
            <div class="row-fluid">
                <div class="span9">
                    <div class="paginator-cont">


                        {{ simple_paginator_render('mycp_list_readonly_reservations',null,{
                'container_class': 'paginator',
                'previousPageText': '«',
                'nextPageText': '»',
                'currentClass': 'current',
                'firstPageText': 'Primera',
                'lastPageText': 'Última',
                'route_params': {'items_per_page':items_per_page, 'filter_booking_number':'null','filter_date_reserve':filter_date_reserve_twig,'filter_offer_number':filter_offer_number,'filter_reference':filter_reference,'filter_date_from':filter_date_from_twig,'filter_date_to':filter_date_to_twig,'filter_status':filter_status,'sort_by':sort_by}
                }) }}

                    </div>
                </div>

                <div class="span3" style="margin-top: 10px" >
                    <div class="row-fluid">
                        <div class="span4" style="margin-top: 4px; font-size: 12px">
                            Organizar:
                        </div>
                        <div class="span8">
                            <select name="sort_by" id="sort_by" onchange="javascript:action_submit()" class="input-block-level pull-right">
                                {{ render(controller('mycpBundle:BackendUtils:orderBy', {'sortBy': sort_by, 'elementToOrder': constant('\\MyCp\\mycpBundle\\Helpers\\ElementToOrder::RESERVATION_LODGING_MODULE')})) }}
                            </select>
                        </div>
                    </div>

                </div>
            </div>
            </form>
            <table class="table table-hover table-striped" style="font-size: 12px">
                <thead>
                <tr>
                    <th>Fecha</th>
                    <th>Número de reserva</th>
                    <th>Habit.</th>
                    <th>Huésp.</th>
                    <th>Llegada</th>
                    <th>Noches</th>
                    <th>Precio</th>
                    <th>Estado</th>
                    <th style="text-align: right">Acciones</th>
                </tr>
                </thead>
                <tbody>
                {% set flag=0 %}
                {%set pending = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_PENDING')%}
                {%set available = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_AVAILABLE')%}
                {%set reserved = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_RESERVED')%}
                {%set not_available = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_NOT_AVAILABLE')%}
                {%set partially_available = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_PARTIAL_AVAILABLE')%}
                {%set partially_reserved = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_PARTIAL_RESERVED')%}
                {%set cancelled = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_CANCELLED')%}
                {%set partially_cancelled = constant('\\MyCp\\mycpBundle\\Entity\\generalReservation::STATUS_PARTIAL_CANCELLED')%}

                {% for reservation in reservations %}
                <tr class="{% if reservation[0].gen_res_status==available %}warning{%endif%}{% if reservation[0].gen_res_status==reserved %}error{%endif%}{% if reservation[0].gen_res_status==not_available or reservation[0].gen_res_status==cancelled or reservation[0].gen_res_status==partially_cancelled%}info{%endif%}">
                    <td>{{reservation[0].gen_res_date |date("d/m/Y")}}</td>
                    <td>{{reservation[0].gen_res_id | getCASId}}</td>
                    <td>{{reservation[1]}}</td>
                    <td>{{reservation[3]+reservation[5]}}</td>
                    <td>{{reservation[0].gen_res_from_date|date("d/m/Y")}}</td>
                    <td>{{total_nights[loop.index0]}}</td>
                    <td>{{ reservation[0].gen_res_total_in_site }} CUC {{reservation[0].gen_res_status}}</td>
                    <td>
                        {{ render(controller('mycpBundle:BackendReservation:getGeneralReservationStatusName', {'status': reservation[0].gen_res_status, 'wrap': false, 'showInDiv': true})) }}
                    </td>
                    <td style="text-align: right">
                        <a class="btn btn-mini" href="{{ path('mycp_details_readonly_reservation',{'id_reservation':reservation[0].gen_res_id}) }}">Detalles</a>
                    </td>
                </tr>
                {% set flag = flag + 1%}
                {% endfor %}
                </tbody>
            </table>
            <div class="row-fluid">
                <div class="paginator-cont span9">
                    {{ simple_paginator_render('mycp_list_reservations',null,{
                'container_class': 'paginator',
                'previousPageText': '«',
                'nextPageText': '»',
                'currentClass': 'current',
                'firstPageText': 'Primera',
                'lastPageText': 'Última',
                'route_params': {'items_per_page':items_per_page, 'filter_booking_number':'null','filter_date_reserve':filter_date_reserve_twig,'filter_offer_number':filter_offer_number,'filter_reference':filter_reference,'filter_date_from':filter_date_from_twig,'filter_date_to':filter_date_to_twig,'filter_status':filter_status,'sort_by':sort_by}
                }) }}
                </div>
            </div>
            {% else %}
            <p> <div class="alert alert-block">No hay reservas disponibles.</div></p>
            {% endif %}
        </div>

    </div>
<link href="{{ asset('bundles/mycp/css/datepicker.css') }}" rel="stylesheet" type="text/css" />
<script src="{{ asset('bundles/mycp/js/bootstrap-datepicker.js') }}" type="text/javascript"></script>
<script type="text/javascript" src="{{ asset('bundles/mycp/js/bootstrap-datepicker.es.js') }}" charset="UTF-8"></script>
<script>
    $('.datepicker').datepicker({
        format: 'dd/mm/yyyy',
        todayBtn: 'linked',
        language: 'es',
        autoclose: true
    });

    function submit_filters(ev)
    {
        if(ev.keyCode==13)
        {
            action_submit();
        }
    }

    function action_submit()
    {
        items_per_page={{items_per_page}};
        filter_date_reserve=$('#filter_date_reserve').val();
        if(filter_date_reserve=='')filter_date_reserve='null';
        filter_offer_number=$('#filter_offer_number').val();
        if(filter_offer_number=='')filter_offer_number='null';
        filter_status=$('#filter_status').val();
        if(filter_status=='')filter_status='null';
        filter_booking_number='null';//$('#filter_booking_number').val();
        //if(filter_booking_number=='')filter_booking_number='null';
        filter_reference='null';//$('#filter_reference').val();
        //if(filter_reference=='')filter_reference='null';
        filter_date_from=$('#filter_date_from').val();
        if(filter_date_from=='')filter_date_from='null';
        filter_date_to=$('#filter_date_to').val();
        if(filter_date_to=='')filter_date_to='null';
        sort_by=$('#sort_by').val();
        if(sort_by=='')sort_by='null';
        filter_date_from=filter_date_from.replace('/',"_");
        filter_date_from=filter_date_from.replace('/',"_");
        filter_date_to=filter_date_to.replace('/',"_");
        filter_date_to=filter_date_to.replace('/',"_");
        filter_date_reserve=filter_date_reserve.replace('/',"_");
        filter_date_reserve=filter_date_reserve.replace('/',"_");
        url_submit=$('#filter').attr('action');
        url_submit=url_submit+'/'+items_per_page+'/'+filter_date_reserve+'/'+filter_offer_number+'/'+filter_reference+'/'+filter_date_from+'/'+filter_date_to+'/'+sort_by+'/'+filter_booking_number+'/'+filter_status;
        $('#filter').attr('action',url_submit);
        $('#filter').submit();
    }

</script>
{% include 'mycpBundle:utils:confirmation_window.html.twig' with {'message':'¿Confirma que desea eliminar el elemento?','confirmation_action_label':'Eliminar'}%}
{% endblock %}
{% block javascripts %}
<script type="text/javascript" src="{{ asset('bundles/mycp/js/common_list.js') }}"></script>
{% endblock %}
