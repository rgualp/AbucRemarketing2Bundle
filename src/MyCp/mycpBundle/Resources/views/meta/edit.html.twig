{% extends 'mycpBundle:layout:backend.html.twig' %}
{% block title %}Administración{% endblock %}
{% block top_body %}
    <ul class="breadcrumb">
        <li><a href="{{path('mycp_backend_front')}}">Administración</a> <span class="divider">/</span></li>
        <li class="active">Meta Tags</li>
    </ul>
{% endblock %}
{% block form_actions %}
    <ul class="breadcrumb">
        <li><a href="{{path('mycp_backend_front')}}">Administración</a> <span class="divider">/</span></li>
        <li class="active">Meta Tags</li>
    </ul>
    <div class="form-actions">
        <button class="btn btn-success" type="submit">Guardar</button>
        <a class="btn" href="{{ path('mycp_backend_front') }}">Cancelar</a>
    </div>
    </form>
{% endblock %}
{% block content %}
<h2>Editar Meta Tags ({{metaTag.metaTitle}})</h2>
<hr/>
<form class="sfgen" method="POST" action="{{ path('mycp_edit_metatags',{'meta_id':meta_id}) }}" novalidate="true">
    {{form_widget(form)}}
{% endblock %}