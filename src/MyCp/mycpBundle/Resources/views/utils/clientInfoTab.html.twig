<ul class="nav nav-tabs">
        <li class="active">
            <a href="#reservas" data-toggle="tab">Cliente</a>
        </li>
    </ul>
    <table class="table table-striped" style="font-size: 12px;">
        <tr>
            <td><b>Nombre:</b> {{user.getUserUserName}} {{user.getUserLastName}}</td>
            <td><b>Correo:</b> {{user.getUserEmail}}</td>
            <td><b>País:</b> {{user.getUserCountry.getCoName}}</td>
        </tr>
        <tr>
            <td><b>Ciudad:</b> {%if user.getUserCity != null%}{{user.getUserCity}}{%else%}-{%endif%}</td>
            <td colspan="2"><b>Dirección:</b> {%if user.getUserAddress != null%}{{user.getUserAddress}}{%else%}-{%endif%}</td>
        </tr>
    </table>