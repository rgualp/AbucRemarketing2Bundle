{% if isFromUserWithReservation %}
    <br/>¿Es un comentario positivo?
    <a class="changePositive btn btn-mini {% if comment.isPositive %}btn-success{% endif %}" data-id="{{ comment.comId }}" data-url="{{ path("mycp_change_positive") }}" data-positive="1"><i class="fa fa-thumbs-o-up"></i></a>
    <a class="changePositive btn btn-mini {% if not comment.isPositive %}btn-danger{% endif %}" data-id="{{ comment.comId }}" data-url="{{ path("mycp_change_positive") }}"  data-positive="0"><i class="fa fa-thumbs-o-down"></i></a>
{% endif %}

<script>
    $(".changePositive").click(function(){

    });
</script>