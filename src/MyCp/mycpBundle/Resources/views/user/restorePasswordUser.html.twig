{% extends 'mycpBundle:layout:base.html.twig' %}
{% block title %}Autenticarse{% endblock %}
{% block top_body %}
    <ul class="breadcrumb">
        <li><a href="{{ path("backend_login") }}">Inicio</a> <span class="divider">/</span></li>
        <li class="active">¿Olvidó contraseña?</li>
    </ul>
{% endblock %}
{% block body %}
    <div class="row-fluid ">

        <div class="span6 offset3">
            <div class="well ">
                <h2 class="form">¿Olvidó contraseña?</h2>
                <hr/>
                {% for message in app.session.flashbag.get('message_ok') %}
                    <div class="alert alert-info">{{ message }}</div>
                {% endfor %}
                {% for error in app.session.flashbag.get('message_error') %}
                    <div class="alert alert-error">{{ error }}</div>
                {% endfor %}
            <form novalidate class="form-horizontal" action="{{ path("mycp_forget_password") }}" method="post">
                <div class="control-group">
                    <label class="control-label" for="inputUsername">{{ form_label(form.user_email) }}</label>
                    <div class="controls">
                        {{ form_widget(form.user_email) }}
                        {{ form_errors(form.user_email) }}
                    </div>
                    {{ form_rest(form)}}
                </div>
                <div class="control-group">
                    <div class="controls">
                        <button class="btn btn-success" type="submit">Resetear contraseña</button>
                    </div>
                </div>
            </form>
                </div>
            </div>

        </div>
{%endblock%}
