[
{% set it = 0 %}
{% for actualReservation in reservations %}
    {% if it != 0 %},{% endif %}{
    "title": {%if actualReservation.status == constant('\\MyCp\\mycpBundle\\Entity\\ownershipReservation::STATUS_CANCELLED')%}"Cancelada"{% else %}"Reservada"{% endif %},
    "date": "{{ actualReservation.date | date("Y-m-d") }}",
    "classname": {%if actualReservation.status == constant('\\MyCp\\mycpBundle\\Entity\\ownershipReservation::STATUS_CANCELLED')%}"redTag"{%else%}"blueTag"{%endif%},
    "badge":false
    }
    {% set it = it + 1 %}
{% endfor %}
{% if detailCount > 0 and reservationCount > 0%},{% endif %}
{% set it = 0 %}
{% for date in details %}
        {% if it != 0 %},{% endif %}
        {
        "title": "No disponible",
        "date": "{{ date | date("Y-m-d")}}",
        "classname":"orangeTag",
        "badge":false
        }
        {% set it = it + 1 %}
{% endfor %}
]