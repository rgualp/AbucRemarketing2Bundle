<div class="col-xs-12">
    <ul class="nav nav-pills nav-pills-new">
        {#<li class="pull-right"><span style="font-size: 8px; color: #F4981D;     margin-left: -15px; position: absolute;"><b>{{ 'NEW'|trans|upper }}</b></span><a title="{{"JOIN_CASA" | trans }}" href="{{ path('my_cp_casa_module_registration')}}">{{"JOIN_CASA"| trans }}</a></li>#}
        <li class="dropdown">
            <a title="{{"LANGUAGE" | trans }}" data-toggle="collapse" data-target=".dropdown-lang" class="dropdown-toggle" href="#"><img class="icon tiny-lang-icon" src="{{ asset('bundles/frontend/img/icons/pixel.gif')}}" alt="{{"Pixel" | trans}}"/>
                {{(app.session.get('app_lang_name') == null or app.session.get('app_lang_name') == "") ? "Español" : app.session.get('app_lang_name')}}</a>
            <ul class="dropdown-menu dropdown-lang">
                {{ render_esi(controller('FrontEndBundle:Language:getLanguages', {'route': route, 'routeParams': routeParams})) }}
            </ul>
        </li>
        <li class="dropdown">
            <a title="{{"CURRENCY" | trans }}" data-toggle="collapse" data-target=".dropdown-currency" class="dropdown-toggle" href="#"><img class="icon tiny-curr-icon" src="{{ asset('bundles/frontend/img/icons/pixel.gif')}}" alt="{{"Pixel" | trans}}"/>
                {{(app.session.get('curr_acronym') == null or app.session.get('curr_acronym') == "") ? default_currency().currcode|upper : app.session.get('curr_acronym')|upper}}</a>
            <ul class="dropdown-menu dropdown-currency">
                {{ render_esi(controller('FrontEndBundle:Currency:getCurrencies', {'params': app.request.attributes.all, 'route': route, 'routeParams': routeParams})) }}
            </ul>
        </li>
    </ul>
</div>