{% extends 'MyCpMobileFrontendBundle::base.html.twig' %}
{% set section = constant('\\MyCp\\mycpBundle\\Entity\\metaTag::SECTION_MYCASATRIP') %}
{% block title %}MyCasaTrip{% endblock %}
{% block body %}
    <div class="space space-5"></div>
    <div class="container">
        {% for message in app.session.flashbag.get('message') %}
            <div class="alert alert-info" style="margin-bottom: 10px">{{ message }}</div>
        {% endfor %}
        <div class="row">
            {% if menu is not defined %}
                {% set menu = 0 %}
            {% endif %}
            {{ render(controller('FrontEndBundle:Mycasatrip:getMenuCount', {'menu_selected': menu})) }}

            {% block body_inner %}{% endblock %}
        </div>
    </div>

    <div class="tabbable col-xs-12 banner-dow" onclick="window.location='{{ path('frontend_mycasatrip_apps') }}';">
        <div class="row">
            {#<img src="{{ asset('bundles/frontend/img/mycasatrip/b1.png') }}">#}
            <div id="div_b" style="float: right; vertical-align: middle; margin-right: 50px; margin-top: 55px">
                <h4>{{ 'find_reservations'|trans }}</h4>
                <h1>MyCasa Trip APP</h1>
            </div>
        </div>
    </div>
    <script>

    </script>

    </div>

{% endblock %}
