{% extends 'PartnerBundle:Mail:translateMailTemplate.html.twig' %}
{% block content %}
        <h4 style="font-family: Arial; font-size: 20px">{{"DEAR_MR"|trans({}, "messages", user_locale)}} {{user.getUserUserName|capitalize}} {{user.getUserLastName|capitalize}}</h4>

        {{"EXPIRED_OFFER_TEXT_2"|trans({}, "messages", user_locale) | raw}}.
        </p>
        <p style="font-size: 14px;font-family: arial"> {{"EXPIRED_OFFER_TEXT_3"|trans({}, "messages", user_locale)}}</p>

        {% for reservation in reservations %}
            {{ include('PartnerBundle:Mail:reservationControlRemarketing.html.twig', {"index" : loop.index0, "reservation": reservation, 'photos': photos}) }}
            <br/>
        {% endfor %}

    <h4 style="font-size: 16px; font-weight: normal; font-style: italic">
        {{"EMAIL_GREETINGS" | trans({}, "messages", user_locale)}}.<br/>
        {{"EMAIL_GREETINGS_2" | trans({}, "messages", user_locale)}},<br/>
    </h4>
{% endblock %}
