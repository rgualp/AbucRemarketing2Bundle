{% for language in languages %}
    <li><a rel="nofollow" href="{{ path('frontend_change_language_callback',{'lang': language.getLangCode|lower, 'route': route, 'routeParams': routeParams }) }}">{{ language.getLangName }}</a></li>
{% endfor %}

