{% extends 'FrontEndBundle:layout:baseMailTemplate2.html.twig' %}

{% block content %}
    <p style="font-family: Arial; font-size: 14px">
        <p>Hola, {% if (user_name is defined) %}{{  user_name  }}:{% endif %}</p>
        {{ content | raw }}
    </p>
{% endblock %}