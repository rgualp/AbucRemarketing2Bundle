<div id="myCarousel" class="carousel slide">
    <div class="camera_wrap camera_azure_skin" id="camera_wrap_1">
        {% for slide in slides %}
            <div data-thumb="{{ asset(slide)}}" data-src="{{ asset(slide)}}">
            </div>
        {% endfor %}
    </div><!-- #camera_wrap_1 -->
</div> <!-- /.carousel -->
