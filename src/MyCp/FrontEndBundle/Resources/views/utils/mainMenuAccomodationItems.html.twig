<li class="nav-item dropdown" >

    <a class="nav-link dropdown-toggle" title="My Casa Particular - {{ "SEARCH_MAIN_MENU" | trans }}" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        {{ "SEARCH_MAIN_MENU" | trans }}
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        {% set counter = 0 %}
        <div class="row-fluid  ">
            <table style="width: 280px" class="table-responsive-md dropdown-table">
                <tbody>
                {% for prov in provinces %}
                    {% if counter == 0 %}
                        <tr>
                    {% endif %}
                    <td>
                        <a href="{{ path('frontend_search_ownership',{'text':for_url[prov.prov_id]}) }}">{{ for_name[prov.prov_id] }}</a>
                    </td>
                    {% if counter == 1 %}
                        </tr>
                        {% set counter = 0 %}
                    {% else %}
                        {% set counter = counter + 1 %}
                    {% endif %}
                {% endfor %}
                </tbody>
            </table>
        </div>

    </div>
</li>